## 2025-02-24 - Missing ARIA Labels on Icon-only Buttons
**Learning:** Many icon-only buttons (like menu toggles, notification bells, and send buttons) using `data-lucide` lack accessible names, making them difficult for screen reader users to understand.
**Action:** Always verify that elements containing only icons or visual indicators include an appropriate `aria-label` attribute (in Italian, to match the app's context).
