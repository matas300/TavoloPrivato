## 2024-05-24 - [Accessible Icon Buttons]
**Learning:** Icon-only interactive elements lacking ARIA labels are a recurring accessibility gap across the site.
**Action:** Ensure missing `aria-label` attributes are added to all non-text `<button>` and `<a>` elements for improved screen reader support, localized into Italian.
