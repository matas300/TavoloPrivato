## 2024-06-20 - Add ARIA Labels to Icon-Only Buttons
**Learning:** Many icon-only interactive elements (such as `menu-toggle` and `btn-icon`) lack screen-reader accessibility, limiting usability for vision-impaired users.
**Action:** Added `aria-label` attributes to these components to ensure their purpose is communicated via assistive technologies, matching Italian localization.
