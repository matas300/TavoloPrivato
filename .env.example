DATABASE_URL="file:./dev.db"
PORT=3000
STRIPE_MODE=mock
# STRIPE_SECRET_KEY=***   # richiesto solo se STRIPE_MODE=live
# STRIPE_WEBHOOK_SECRET=*** # richiesto solo se STRIPE_MODE=live (HMAC signature verification)
